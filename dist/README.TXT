Place in your plugins folder and run the server.

After that, edit the ChatRooms.properties file and add names to the adminnames attribute sepparated by a comma (eg. playername1,playername2,playername3).

Restart the server.